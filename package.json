{
  "name": "consultant-onepager",
  "version": "1.0.0",
  "private": true,
  "description": "One-pager website for an independent consultant. Static HTML + Tailwind CSS.",
  "scripts": {
    "build": "tailwindcss -i src/css/input.css -o css/style.css --minify",
    "watch": "tailwindcss -i src/css/input.css -o css/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
